# Ensure /nix mountpoint exists (fallback, normally baked into image).
# Ensure /var/nix exists as the persistent backing store for Nix across boots.
d /nix     0755 root root -
d /var/nix 0755 root root -